version: '3.9'

services:
  postgres:
    image: postgres:16.1
    container_name: shareit-postgres
#    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: shareit
    ports:
      - "5432:5432"
#    volumes:
#      - shareit-data:/var/lib/postgresql/data
#
#volumes:
#  shareit-data:
